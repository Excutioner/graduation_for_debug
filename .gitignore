# --- 1. 全局忽略 (默认不上传任何东西) ---
*

# --- 2. 白名单 (只允许以下内容“突围”) ---
!*/
!main.py
!tracking/**
!utils/**
!.gitignore

# --- 3. 强力过滤 (防止白名单里混入垃圾) ---
# 注意：这些必须放在白名单之后，否则无效
__pycache__/
*.pyc
.DS_Store
.vscode/
*.txt